{
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "prepare": "npm run bootstrap",
    "build-packages": "lerna run build",
    "copy-public": "node scripts/copy-public.js",
    "copy-static": "node scripts/copy-static.js",
    "build": "npm run build-packages && npm run copy-public && npm run copy-static"
  },
  "devDependencies": {
    "fs-extra": "^7.0.1",
    "glob": "^7.1.3",
    "lerna": "^3.13.1"
  }
}
